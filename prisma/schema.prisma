generator client {
  provider = "prisma-client"
  output   = "./generated"
}

datasource db {
  provider = "postgresql"
}

enum UserRole {
  USER
  ADMIN
}

enum Difficulty {
  EASY
  MEDIUM
  HARD
}

model User {
  id             String          @id @default(uuid())
  clerkId        String          @unique
  email          String          @unique
  role           UserRole        @default(USER)
  firstName      String?
  lastName       String?
  imageUrl       String?
  problems       Problem[]
  createdAt      DateTime        @default(now())
  updatedAt      DateTime        @updatedAt
  solvedProblems ProblemSolved[]
  submissions    Submission[]

  @@index([role])
}

model Problem {
  id                 String          @id @default(uuid())
  title              String
  description        String
  difficulty         Difficulty
  examples           Json // Language-specific examples
  constraints        String
  tags               String[]
  hints              String?
  editorial          String?
  testCases          Json
  codeSnippets       Json
  referenceSolutions Json
  userId             String
  createdAt          DateTime        @default(now())
  updatedAt          DateTime        @updatedAt
  user               User            @relation(fields: [userId], references: [id], onDelete: Cascade)
  solvedBy           ProblemSolved[]
  submissions        Submission[]

  @@index([difficulty])
}

model Submission {
  id            String   @id @default(uuid())
  userId        String
  problemId     String
  sourceCode    Json
  language      String
  stdin         String?
  stdout        String?
  stderr        String?
  compileOutput String?
  status        String
  memory        String?
  time          String?
  createdAt     DateTime @default(now())

  user    User    @relation(fields: [userId], references: [id], onDelete: Cascade)
  problem Problem @relation(fields: [problemId], references: [id], onDelete: Cascade)
  // testCases TestCaseResult[]

  @@index([status])
}



model ProblemSolved {
  id        String @id @default(uuid())
  userId    String
  problemId String

  user      User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  problem   Problem  @relation(fields: [problemId], references: [id], onDelete: Cascade) //If a User is deleted → all their ProblemSolved records get deleted automatically.
  createdAt DateTime @default(now())

  @@unique([userId, problemId])
}
